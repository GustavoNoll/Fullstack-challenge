// config.js
const config = {
  backendUrl: 'http://localhost:3000', // Altere para a URL do seu backend
  endpoints: {
    files: '/api/files',
    users: '/api/users'
  }
};

export default config;
